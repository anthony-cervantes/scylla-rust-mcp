## Purpose
Explain the goal of this PR and link related issues.

## Changes
- 

## Testing
Describe how you tested the changes. Include commands/log output when helpful.

## Checklist
- [ ] `cargo fmt --all` passes
- [ ] `cargo clippy --all-targets -- -D warnings` passes
- [ ] `cargo test` passes (integration tests may remain ignored unless `SCYLLA_URI` is set)
- [ ] Docs/README updated if needed
- [ ] No secrets or credentials added (see `.gitignore` and `SECURITY.md`)

## Notes
Anything else for reviewers to be aware of.

