# Example environment variables for scylla-rust-mcp

# Required: address of your Scylla/Cassandra node
SCYLLA_URI=127.0.0.1:9042

# Optional credentials
# SCYLLA_USER=scylla
# SCYLLA_PASS=<password>

# Enable TLS (OpenSSL). If true/1, optionally set a CA bundle.
# SCYLLA_SSL=true
# SCYLLA_CA_BUNDLE=/absolute/path/to/ca.pem
# SCYLLA_SSL_INSECURE=false  # dev only; disables peer verification

# Logging level for the server
# RUST_LOG=info

